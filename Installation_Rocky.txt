# Schlüsselpaar erstellen
ssh-keygen -t rsa -N ''

# Schlüssel kopieren
sh-copy-id kurs0@172.16.18.1

# Pakete installieren
sudo yum install python3-pip
sudo yum install python3-devel
sudo pip3 pygame matplotlib geopy ephem scipy numpy Cython pyinstaller

https://zoom.us/download?os=linux
